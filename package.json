{
  "name": "syncho-stok",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "electron": "npm run electron --prefix frontend",
    "start:electron": "npm run start:electron --prefix frontend",
    "dist": "npm run dist --prefix frontend",
    "build": "npm ci --prefix frontend && npm run build --prefix frontend && cp -r frontend/dist/. backend/public/ && npm ci --prefix backend && npm run build --prefix backend",
    "start": "node backend/dist/server.js"
  }
}
